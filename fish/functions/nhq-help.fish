# nhq-help — thin wrapper: the ONE canonical wiki lives in ~/.local/bin/nhq-help (dots/bin).
function nhq-help --description 'NetrunnersHQ command wiki (canonical: dots/bin/nhq-help)'
    ~/.local/bin/nhq-help $argv
end
