# never commit local secrets
fish/private.fish
**/private.fish
*.secret
.env
.env.*
# firecrawl rotator key store (lives at ~/.config/firecrawl-keys — never commit a key)
firecrawl-keys
firecrawl-rotator-keys*
