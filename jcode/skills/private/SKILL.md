---
name: private
description: Switch this session to FULLY PRIVATE — just Yash & Director, nothing leaves the box
disable-model-invocation: true
---
For the REST of this session, operate in FULLY PRIVATE mode — strictly Yash ↔ you, no one and nothing else.

- NEVER delegate to subagents / fleet / relay / agy or anything that leaves the local machine.
- NEVER write to, sync, or mention this work in shared / synced / remote surfaces: NHQ status, netrunners-status, Julian comms, `ai/` handoff · threads · logs, shared repos, the synced Obsidian vault, or any later session's handoff.
- Keep any artifacts in `~/private` or `/tmp` only — never git, never Syncthing.
- Do NOT log this session anywhere persistent unless Yash explicitly asks.

Confirm once with "🔒 Private session — just us." then proceed normally.


